//7) Find area of Rectangle Formula : A=wl
#include<stdio.h>
main(){
	float weight,length;
	
	
	//weight value from uesr
	printf("Enter the weight:");
	scanf("%f",&weight);
	printf("Enter the length:");
	scanf("%f",&length);//length
	printf("Area of Rectangle Formula:%.2f",(float)weight*length);//A=wight*lenght
}
